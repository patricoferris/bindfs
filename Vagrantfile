# -*- mode: ruby -*-
# vi: set ft=ruby :

# This is a temporary manually maintained VM for working on FUSE 3 support.
# TODO: replace this with a test box under vagrant/ after completing the fuse3 port

Vagrant.configure("2") do |config|
  config.vm.box = "debian/contrib-buster64"
end
